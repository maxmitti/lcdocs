<?xml version="1.0" encoding="ISO-8859-1" standalone="yes"?>
<?xml-stylesheet type="text/xsl" href="../../../clonk.xsl"?>
<clonkDoc xmlns="https://clonkspot.org"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="https://clonkspot.org ../../../clonk.xsd" xml:lang="de">
        <func isAsync="false">
                <title>TrainPhysical</title>
                <category>Objekte/Status</category>
                <versions><version>4.9.6.0 CR</version></versions>
                <syntax>
                        <rtype isReference="false">bool</rtype>
                        <params>
                                <param isReference="false" isOptional="false"><type>string</type><name>szPhysical</name><desc>Name der Eigenschaft, die geändert werden soll</desc></param>
                                <param isReference="false" isOptional="false"><type>int</type><name>iTrainBy</name><desc>0-100000: Wert, um den trainiert wird</desc></param>
                                <param isReference="false" isOptional="false"><type>int</type><name>iMaxTrain</name><desc>Wert, der durch das Training nie überschritten wird. Es wird allerdings in keinem Fall die Physical reduziert, auch wenn der Zielwert unter dem hier angegebenen Wert läge.<br /></desc></param>
				<param isReference="false" isOptional="true"><type>object</type><name>pObj</name><desc>Objekt, dessen Physical trainiert werden soll. 0 bei lokalen Aufrufen.</desc></param>
			</params>
		</syntax>
                <desc>Erhöht den Wert der physischen Eigenschaft eines Objekts um den in iTrainBy angegebenen Wert. Mit diesem Aufruf werden sowohl die permanenten, als auch temporäre und temporär gestapelte Physicals trainiert. Dieser Befehl kann beispielsweise genutzt werden, um nach einem Zauber die Magiephysical zu trainieren.</desc>
		<examples>
			<example>
<code>// Globaler Aufruf, wenn ein Zauber erfolgreich war: Magie trainieren
global func OnClonkSucceededSpell(id idSpell, object pClonk)
  {
  if (!pClonk) if (!(pClonk = <funcLink>this</funcLink>())) <funcLink>return</funcLink>(<funcLink>_inherited</funcLink>(idSpell));
  // Zauberkraft trainieren (plus 1/10 des Zauberwerts)
  if (!<funcLink>ObjectCount</funcLink>(NMGE))
    {
    var iMaxTrain, idInfo;
    // Maximales Training bei Infoobjekten: Nach Ursprungsdefinition richten
    if (idInfo = <funcLink>GetObjectInfoCoreVal</funcLink>("id", "ObjectInfo", pClonk)) iMaxTrain = idInfo->~MaxMagicPhysical();
    // Infodefinition hat kein MaxMagicPhysical (d.h., kein Zauberclonk) oder kein Infoobjekt vorhanden:
    // Training wird eh nicht permanent gesichert; nach aktueller Definition richten
    if (!iMaxTrain) iMaxTrain = pClonk->~MaxMagicPhysical();
    // Trainieren
    TrainPhysical("Magic", <funcLink>Value</funcLink>(idSpell)/MCLK_ManaTrainRate, iMaxTrain, pClonk);
    }
  // Gegebenenfalls weitere Hooks erlauben
  <funcLink>return</funcLink> (<funcLink>_inherited</funcLink>(idSpell, pClonk));
  }</code>
				<text>Beispiel aus der System.c4g: Trainiert bei einem erfolgreichen Zauber die Magiephsical des Zauberers. Dabei wird sicher gestellt, dass der zaubernde Clonk nicht über das durch die Funktion MaxMagicPhysical() in seiner Definition angegebene Limit trainiert werden kann.</text>
			</example>
		</examples>
                <related>
			<emLink href="definition/defcore.html#SektionPhysical">DefCore.txt, [Physical]</emLink>
                        <funcLink>GetPhysical</funcLink>
                        <funcLink>SetPhysical</funcLink>
			<funcLink>ResetPhysical</funcLink>
                </related>
        </func>
        <author>Sven2</author><date>2003-06</date>
</clonkDoc>

